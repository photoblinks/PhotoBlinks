/** Mirrors the database trigger in
 * 20260915000000_photographer_location_collections.sql, which is the real
 * enforcement; this copy only drives UI messaging and the early app check. */
export const MAX_SHARE_LOCATIONS = 10;
